
usage: rbmk cat FILE...

Concatenate files and print on the standard output.

For example, the following invocation concatenates the content
of the `file1.txt` and `file2.txt` files to the stdout:

    $ rbmk cat file.txt

This command exits with `0` on success and `1` on failure.
